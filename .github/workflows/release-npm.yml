name: Release to npm

on:
  push:
    tags:
      - 'v*.*.*'

jobs:
  release-npm:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          registry-url: 'https://registry.npmjs.org'

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: Install dependencies
        run: pnpm install

      - name: Build package
        run: pnpm --filter=@ververv/oaktis-sdk run build

      - name: Update version
        run: npm version ${GITHUB_REF_NAME#v} --no-git-tag-version
        working-directory: packages/js-sdk

      - name: Publish to npm
        run: npm publish --access public
        working-directory: packages/js-sdk
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          name: Release ${{ github.ref_name }}
          body: |
            ## 📦 npm Package Released

            **@ververv/oaktis-sdk** version ${{ github.ref_name }} has been published to npm.

            ### Install
            ```bash
            npm install @ververv/oaktis-sdk@${{ github.ref_name }}
            ```

            ### Links
            - 📦 [npm Package](https://www.npmjs.com/package/@ververv/oaktis-sdk)
            - 📚 [Documentation](https://docs.oaktis.com)
            - 🐙 [GitHub](https://github.com/Oaktis/Oaktis)
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

{
  "name": "oaktis-monorepo",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm -w @oaktis/sdk run build && npm -w oaktis-cli run build",
    "lint": "echo 'Linting...'",
    "test": "npm -w @oaktis/sdk run test",
    "release": "echo 'Use Git tags to trigger CI'"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  }
}

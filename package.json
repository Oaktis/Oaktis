{
  "name": "oaktis-monorepo",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "pnpm --filter=@ververv/oaktis-sdk run build && pnpm --filter=oaktis-cli run build",
    "lint": "echo 'Linting...'",
    "test": "pnpm --filter=@ververv/oaktis-sdk run test",
    "release": "echo 'Use Git tags to trigger CI'"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  }
}
